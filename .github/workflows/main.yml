name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: List files
        run: cat ./index.html; ls ~/*
      - name: Copy files
        run: ssh -V
           
        
